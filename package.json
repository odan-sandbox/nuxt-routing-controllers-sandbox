{
  "name": "nuxt-routing-controllers-sandbox",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^1.8.0",
    "eslint": "^5.16.0",
    "eslint-config-prettier": "^4.2.0",
    "eslint-plugin-prettier": "^3.0.1",
    "prettier": "^1.17.0"
  },
  "scripts": {
    "lint:backend": "yarn eslint --ext .js,.ts --ignore-path .gitignore backend",
    "start:backend": "yarn workspace backend start"
  }
}
